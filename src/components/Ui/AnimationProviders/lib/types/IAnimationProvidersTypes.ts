import { ReactNode } from "react";

export interface IFadeOutProviderProps{
    children: ReactNode;
    yAxis?: number;
}

export interface IFromLeftAnimationProviderProps{
    children: ReactNode;
    xAxis?: number;
}